version: "3.1"

rules:
  - rule: Fallback rule
    steps:
      - intent: nlu_fallback
      - action: utter_repeat_prompt

  - rule: out-of-scope
    steps:
      - intent: out_of_scope
      - action: utter_out_of_scope
